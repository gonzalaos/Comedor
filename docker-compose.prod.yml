services:
  db:
    image: postgres:15-alpine
    container_name: comedor_db_container
    environment:
      POSTGRES_DB: comedor_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: dev-password
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d comedor_db"]
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - postgres_data:/var/lib/postgresql/data

  backend:
    image: ghcr.io/gonzalaos/comedor-backend:latest
    container_name: comedor_backend_container
    depends_on:
      db:
        condition: service_healthy 
    environment:
      SPRING_DATASOURCE_URL: "jdbc:postgresql://db:5432/comedor_db"
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: dev-password
      SPRING_PROFILES_ACTIVE: prod
    restart: unless-stopped

  frontend:
    image: ghcr.io/gonzalaos/comedor-frontend:latest
    container_name: comedor_frontend
    environment:
      - VITE_API_URL=http://localhost/api
    restart: unless-stopped

  ingress:
    image: ghcr.io/gonzalaos/comedor-ingress:latest
    container_name: comedor_ingress
    ports:
      - "80:80"
    depends_on:
      - backend
      - frontend
    restart: unless-stopped

  adminer:
    image: adminer:5.4.0
    container_name: comedor_adminer
    depends_on:
      - db
    ports:
      - "8081:8080"
    restart: unless-stopped

volumes:
  postgres_data: